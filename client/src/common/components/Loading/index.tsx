import React from "react";

import { LoadingWrapper } from "./style";

const Loading = () => (
  /* simplest loading component ever :D */
  <LoadingWrapper>
    Loading ...
  </LoadingWrapper>
);

export default Loading;
